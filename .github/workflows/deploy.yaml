name: Deploy React App to VPS

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to deploy'
        required: true
        default: 'main'
        type: string

jobs:
  # validate:
  #   runs-on: ubuntu-latest
  #   outputs:
  #     branch_exists: ${{ steps.check_branch.outputs.exists }}
  #   steps:
  #   - name: Check if branch exists
  #     id: check_branch
  #     run: |
  #       git ls-remote --heads origin ${{ github.event.inputs.branch }} | grep -q .
  #       if [ $? -eq 0 ]; then
  #         echo "exists=true" >> $GITHUB_OUTPUT
  #       else
  #         echo "exists=false" >> $GITHUB_OUTPUT
  #       fi

  deploy-code:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout selected branch
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.inputs.branch }}
        fetch-depth: 0
      
    - name: Deploy source code to VPS
      uses: appleboy/scp-action@v0.1.3
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USERNAME }}
        password: ${{ secrets.VPS_PASSWORD }}
        port: 22
        source: "."
        target: "/var/www/source"
        
    - name: Build and deploy on VPS
      uses: appleboy/ssh-action@v1
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USERNAME }}
        password: ${{ secrets.VPS_PASSWORD }}
        port: 22
        script: |
          export NVM_DIR=~/.nvm
          source ~/.nvm/nvm.sh
          npm --help
          cd /var/www/source
          sudo rm -rf node_modules
          npm ci
          npm run build
          sudo rm -rf /var/www/html/build/*
          sudo mkdir /var/www/html/build/
          sudo cp -r build/* /var/www/html/build/
          sudo chown -R www-data:www-data /var/www/html/build/
          sudo chmod -R 755 /var/www/html/build/
          echo "React app built and deployed successfully!"

  # branch_not_found:
  #   runs-on: ubuntu-latest
  #   needs: validate
  #   if: needs.validate.outputs.branch_exists == 'false'
  #   steps:
  #   - name: Branch not found
  #     run: |
  #       echo "❌ Branch '${{ github.event.inputs.branch }}' not found"
  #       echo "Please check the branch name and try again"
